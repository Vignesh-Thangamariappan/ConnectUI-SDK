import XCTest
@testable import ConnectUISwift

final class ConnectUISwiftTests: XCTestCase {
    func testExample() throws {
        // Basic test to ensure the library can be imported
        // Since it's a UI wrapper, we just verify it exists
        _ = ConnectView()
    }
}
