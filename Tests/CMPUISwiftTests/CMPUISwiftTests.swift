import XCTest
@testable import CMPUISwift

final class CMPUISwiftTests: XCTestCase {
    func testExample() throws {
        // Basic test to ensure the library can be imported
        _ = CMPUIView()
    }
}
